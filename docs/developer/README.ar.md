# دليل المطوّر
## تثبيت الحزمة

```
pip install -e .
```

### اختبار الحزمة

```
pip install -e ".[tests]"

pytest
```

<!-- ملاحظات الترجمة: "Developer Guide" → "دليل المطوّر" (ترجمة سياقية مناسبة) | "Installing package" → "تثبيت الحزمة" (package=حزمة حسب المسرد) | "Test package" → "اختبار الحزمة" | kept EN: pip, pytest, tests — أسماء أوامر وحزم برمجية لا تُترجم -->